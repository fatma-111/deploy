# --- core API ---------------------------------------------------------------
fastapi==0.115.6
uvicorn[standard]==0.34.0
pydantic==2.10.4
pydantic-settings==2.7.1
python-dotenv==1.0.1
httpx==0.28.1

# --- agent orchestration ----------------------------------------------------
langgraph==0.2.60
langchain-core==0.3.28
langchain-openai==0.2.14

# --- local RAG / knowledge base ----------------------------------------------
# TF-IDF retrieval for the repeated-error cache (app/services/knowledge_base.py).
# Pulls numpy + scipy transitively; no torch, no embedding model, no GPU.
scikit-learn==1.5.2

# --- UI (optional second process) -------------------------------------------
streamlit==1.41.1

# --- tests ------------------------------------------------------------------
pytest==8.3.4
pytest-asyncio==0.25.0
